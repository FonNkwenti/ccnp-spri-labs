hostname PE1
!
! ──────────────────────────────────────────────────────────────────────────────
! Platform: IOS XRv (light, 6.1.x) — XR-mixed capstone retrofit
! Reminder: in XR, '!' is a comment, NOT a sub-mode exit. Use 'exit' or 'root'
! when pushing config line-by-line via netmiko / scrapli. Commit model is
! two-stage: 'commit' applies, 'abort' discards uncommitted candidate config.
! ──────────────────────────────────────────────────────────────────────────────
!
interface Loopback0
 ipv4 address 10.0.0.1 255.255.255.255
!
interface GigabitEthernet0/0/0/0
 description L1 to CE1
 ipv4 address 10.10.111.1 255.255.255.0
 no shutdown
!
interface GigabitEthernet0/0/0/1
 description L2 to P1
 ipv4 address 10.10.12.1 255.255.255.0
 no shutdown
!
interface GigabitEthernet0/0/0/2
 description L3 to P2
 ipv4 address 10.10.13.1 255.255.255.0
 no shutdown
!
end
